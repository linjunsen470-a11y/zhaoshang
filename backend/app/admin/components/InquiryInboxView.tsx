'use client'

import Link from 'next/link'
import { usePathname, useRouter, useSearchParams } from 'next/navigation'
import { FormEvent, useCallback, useEffect, useState } from 'react'
import { LEAD_STATUS_OPTIONS, LEAD_TYPE_LABELS } from '@/collections/shared/fieldOptions'

type Inquiry = { id: string; name: string; phone: string; leadType: string; status: string; businessType: string; budgetRange: string; regionPreference: string; remark: string; internalNote: string; project: { id: string; title: string } | null; generatedProject: { id: string; title: string } | null; createdAt?: string }
type ResponseData = { docs: Inquiry[]; page: number; totalPages: number; totalDocs: number }
const formatter = new Intl.DateTimeFormat('zh-CN', { year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit' })

async function json<T>(response: Response): Promise<T> { const data = await response.json().catch(() => ({})) as T & { error?: string }; if (!response.ok) throw new Error(data.error || '请求失败，请重试'); return data }

export function InquiryInboxView() {
  const router = useRouter(); const pathname = usePathname(); const searchParams = useSearchParams(); const query = searchParams.toString()
  const [data, setData] = useState<ResponseData | null>(null); const [search, setSearch] = useState(searchParams.get('q') || ''); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [message, setMessage] = useState(''); const [editing, setEditing] = useState<Inquiry | null>(null); const [saving, setSaving] = useState(false)
  const updateParams = useCallback((changes: Record<string, string | undefined>) => { setLoading(true); const params = new URLSearchParams(query); Object.entries(changes).forEach(([key, value]) => value ? params.set(key, value) : params.delete(key)); if (!('page' in changes)) params.delete('page'); router.push(`${pathname}${params.size ? `?${params}` : ''}`) }, [pathname, query, router])
  const load = useCallback(async (signal?: AbortSignal) => { setLoading(true); setError(''); try { setData(await json<ResponseData>(await fetch(`/api/admin/inquiries?${query}`, { signal }))) } catch (err) { if (!(err instanceof DOMException && err.name === 'AbortError')) setError(err instanceof Error ? err.message : '咨询加载失败') } finally { if (!signal?.aborted) setLoading(false) } }, [query])
  useEffect(() => { const controller = new AbortController(); fetch(`/api/admin/inquiries?${query}`, { signal: controller.signal }).then(response => json<ResponseData>(response)).then(result => { setData(result); setError('') }).catch(err => { if (!(err instanceof DOMException && err.name === 'AbortError')) setError(err instanceof Error ? err.message : '咨询加载失败') }).finally(() => { if (!controller.signal.aborted) setLoading(false) }); return () => controller.abort() }, [query])
  useEffect(() => { if (!editing) return; const close = (event: KeyboardEvent) => { if (event.key === 'Escape') setEditing(null) }; window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close) }, [editing])

  const save = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!editing) return; const form = new FormData(event.currentTarget); setSaving(true); setError(''); try { await json(await fetch('/api/admin/inquiries', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id: editing.id, status: form.get('status'), internalNote: form.get('internalNote') }) })); setMessage('咨询处理状态已保存'); setEditing(null); await load(); window.setTimeout(() => setMessage(''), 2400) } catch (err) { setError(err instanceof Error ? err.message : '保存失败') } finally { setSaving(false) } }

  return <main className="cms-page" id="main-content"><header className="cms-page-header"><div><p className="cms-eyebrow">轻量收件箱</p><h1>咨询收件箱</h1><p>只记录是否已联系；具体沟通继续在微信或电话中完成。</p></div></header><section className="cms-toolbar"><form className="cms-search" onSubmit={event => { event.preventDefault(); updateParams({ q: search.trim() || undefined }) }}><label className="cms-sr-only" htmlFor="inquiry-search">搜索咨询</label><input id="inquiry-search" value={search} onChange={event => setSearch(event.target.value)} placeholder="搜索称呼、电话或需求…" autoComplete="off" /><button className="cms-button" type="submit">搜索</button></form><label><span>状态</span><select value={searchParams.get('status') || ''} onChange={event => updateParams({ status: event.target.value || undefined })}><option value="">全部</option>{LEAD_STATUS_OPTIONS.map(item => <option value={item.value} key={item.value}>{item.label}</option>)}</select></label><label><span>类型</span><select value={searchParams.get('leadType') || ''} onChange={event => updateParams({ leadType: event.target.value || undefined })}><option value="">全部</option>{Object.entries(LEAD_TYPE_LABELS).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label></section>{error ? <div className="cms-alert" role="alert">{error}</div> : null}<div className="cms-live" aria-live="polite">{message}</div><section className="cms-panel">{loading && !data ? <div className="cms-empty">咨询加载中…</div> : data?.docs.length ? <div className="cms-table-wrap"><table className="cms-table"><thead><tr><th>客户</th><th>类型/需求</th><th>关联房源</th><th>状态</th><th>提交时间</th><th>操作</th></tr></thead><tbody>{data.docs.map(item => <tr key={item.id}><td><strong>{item.name || '未填写称呼'}</strong><small><a href={`tel:${item.phone}`}>{item.phone}</a></small></td><td>{LEAD_TYPE_LABELS[item.leadType] || item.leadType}<small>{[item.businessType, item.budgetRange, item.regionPreference].filter(Boolean).join(' · ') || '暂无补充'}</small></td><td>{item.project?.id ? <Link href={`/admin/collections/projects/${item.project.id}`}>{item.project.title || '查看房源'}</Link> : '自主提交'}{item.generatedProject?.id ? <small><Link href={`/admin/collections/projects/${item.generatedProject.id}`}>已生成房源</Link></small> : null}</td><td><span className={`cms-status cms-status--${item.status}`}>{LEAD_STATUS_OPTIONS.find(option => option.value === item.status)?.label || item.status}</span></td><td>{item.createdAt ? formatter.format(new Date(item.createdAt)) : '—'}</td><td><div className="cms-row-actions"><button type="button" onClick={() => setEditing(item)}>快速处理</button><Link href={`/admin/collections/leads/${item.id}`}>完整详情</Link></div></td></tr>)}</tbody></table></div> : <div className="cms-empty"><h2>暂无咨询</h2><p>小程序产生的新咨询会显示在这里。</p></div>}</section>{data && data.totalPages > 1 ? <nav className="cms-pagination" aria-label="咨询分页"><span>共 {data.totalDocs} 条 · 第 {data.page}/{data.totalPages} 页</span><div><button type="button" disabled={data.page <= 1} onClick={() => updateParams({ page: String(data.page - 1) })}>上一页</button><button type="button" disabled={data.page >= data.totalPages} onClick={() => updateParams({ page: String(data.page + 1) })}>下一页</button></div></nav> : null}{editing ? <div className="cms-drawer-backdrop"><aside className="cms-drawer" role="dialog" aria-modal="true" aria-labelledby="inquiry-edit-title"><header><div><p className="cms-eyebrow">快速处理</p><h2 id="inquiry-edit-title">{editing.name}</h2><p>{editing.phone}</p></div><button className="cms-icon-button" type="button" aria-label="关闭快速处理" onClick={() => setEditing(null)}>×</button></header><form className="cms-form" onSubmit={save}><label>处理状态<select name="status" defaultValue={editing.status}>{LEAD_STATUS_OPTIONS.map(item => <option value={item.value} key={item.value}>{item.label}</option>)}</select></label><label>内部备注<textarea name="internalNote" defaultValue={editing.internalNote} rows={6} maxLength={2000} placeholder="只记录必要的内部事项…" autoComplete="off" /></label><p className="cms-help">用户留言：{editing.remark || '无'}</p><div className="cms-drawer__actions"><button className="cms-button" type="button" onClick={() => setEditing(null)}>取消</button><button className="cms-button cms-button--primary" type="submit" disabled={saving}>{saving ? '保存中…' : '保存处理结果'}</button></div></form></aside></div> : null}</main>
}

export default InquiryInboxView
